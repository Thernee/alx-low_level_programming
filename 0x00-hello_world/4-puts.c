#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (success)
*/
int main(void)
{
	char str[100] = "\"Programming is like building a multilingual puzzle";

	puts(str);
	return (0);
}
