#include "main.h"

/**
 * mul - multiplies 2 integers
 *
 * @a: first parameter passed to mul
 * @b: second integer parameter passed to mul
 * Return: Returns result of mul
 */
int mul(int a, int b)
{
	return (a * b);
}
