#include "main.h"

/**
 * add  - Adds two integer numbers
 *
 * @x: first argument for add function
 * @y: Second argument for add function
 * Return: Returns the result of the addidtion of 2 integers
 */
int add(int x, int y)
{
	int temp = x + y;

	return (temp);
}
