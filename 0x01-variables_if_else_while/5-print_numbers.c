#include <stdio.h>

/**
 * main - Here starts the program
 *
 * Return: Returns 0 always
 */
int main(void)
{
	int number = 0;

	for (number = 0; number < 10; number++)
	{
		printf("%d", number);
	}
	printf("\n");
	return (0);
}
